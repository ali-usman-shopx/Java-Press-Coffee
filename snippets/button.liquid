{%- doc -%}
  Intended for use in a block similar to the button block.

  @param {string} link - link to render
  @param {object} [block] - The block

  @example
  {% render 'button', link: '/collections/all' %}
{%- enddoc -%}

{% assign block_settings = block.settings %}
{% render 'button-custom-styles', block_settings: block_settings, block_id: block.id %}

{% if block_settings.override_hover_css %}
  {% style %}
    .override_hover_button--{{ block.id }}:hover {
      {{ block_settings.hover_css }}
    }
  {% endstyle %}
{% endif %}

<a
  {% if link == blank %}
    role="link"
    aria-disabled="true"
  {% else %}
    href="{{ link }}"
  {% endif %}
  class="
    size-style
    {{ block_settings.style_class }}
    {{ block_settings.style_class }}--{{ block.id }}
    {% if block_settings.override_hover_css %}
      override_hover_button--{{ block.id }}
    {% endif %}
  "
  style="
    {% render 'size-style', settings: block_settings %}
    {% if block.settings.enable_button_font_weight_override %}
      font-weight: {{ block_settings.font_weight }};
    {% endif %}
  "
  {%- if block_settings.open_in_new_tab -%}
    target="_blank"
    rel="noopener noreferrer"
  {%- endif -%}
  {{ block.shopify_attributes }}
>
  {{ block_settings.label }}
</a>
